{
    "random_seed": 42,
    "sampler_train": {
        "name": "SamplerSqrt",
        "params": {
            "digits": 20,
            "sampling_strategy": "uniform_bits"
        }
    },
    "sampler": {
        "name": "SamplerSqrt",
        "params": {
            "digits": 20
        }
    },
    "model_params": {
        "n_positions": 64,
        "n_embd": 384,
        "n_layer": 6,
        "n_head": 6
    },
    "dataset_params": {
        "train_dataset_size": 10000,
        "val_dataset_size": 1000,
        "test_dataset_size": 1000
    },
    "training_params": {
        "batch_size": 256,
        "max_epochs": 200,
        "logging_steps": 4,
        "logging_compute_accuracy": true,
        "early_stopping_patience": 5,
        "early_stopping_threshold": 1e-4
    }
}
